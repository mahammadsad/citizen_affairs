---
import { SITE } from '@utils/constants';
const destination = `${SITE.basePath}en/disclaimer/`;
---
<meta http-equiv="refresh" content={`0;url=${destination}`} /><a href={destination}>Disclaimer</a>

