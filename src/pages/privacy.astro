---
import { SITE } from '@utils/constants';
const destination = `${SITE.basePath}en/privacy/`;
---
<meta http-equiv="refresh" content={`0;url=${destination}`} /><a href={destination}>Privacy Policy</a>
